import React from 'react';

// Placeholder component - MatchCreator temporarily disabled due to TypeScript errors
const MatchCreator: React.FC = () => {
  return (
    <div className="p-4">
      <h2 className="text-xl font-bold text-red-600">MatchCreator Temporarily Disabled</h2>
      <p>This component has TypeScript errors and needs to be refactored.</p>
    </div>
  );
};

export default MatchCreator;
